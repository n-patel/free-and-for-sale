<!DOCTYPE html>
<html lang="en">
<head>
  <title>Clothing</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <link rel="stylesheet" type="text/css" href="/app/assets/stylesheets/application.css">
  <%= stylesheet_link_tag "application.css" %>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>


</head>
<body>

<div class="header">
  Free &amp; For Sale
</div>
<nav class="navbar navbar-default">
  <div class="container">
    <div>
      <ul class="nav navbar-nav">
        <li><a href="/clothing/index">Clothing</a></li>
        <li><a href="../../furniture/index">Furniture</a></li>
        <li><a href="../../electronics/index">Electronics</a></li>
        <li><a href="../../housing/index">Housing</a></li>
        <li><a href="../../school/index">School</a></li>
        <li><a href="../../services/index">Services</a></li>
        <li><a href="../../misc/index">Misc</a></li>
      </ul>
    </div>
  </div>
</nav>

<!--
  add item buttom
  add item page
  login page
  logout button
  @user name + credits or login

  -->

  <div class="top_space"> </div>
<div class="container">
  <% @items.each do |item| %>
    <% if item.category == "clothing" %>
      <div class="panel panel-default item-entry">

          <div class="panel-heading">
            <h2 class="item-title">
              <%= item.title %>
              <small>Posted by <%= item.user.name || item.user_id %> on <%= item.created_at.to_time.strftime('%b %-d, %Y') %></small>
            </h2>
          </div>

          <div class="panel-body item-body">
            <!-- <div class="item-entry_photo col-xs-4">
              <%= item.photos %>
            </div> -->
            <div class="row">
              <div class="col-xs-6">
                <h4>Item Summary</h4>
                <p><%= item.summary %></p>
                <h4>Pickup Location</h4>
                <p><%= item.location %></p>
              </div>

              <div class="col-xs-6">
                <h4>Item Discussion</h4>

                <% @comments.each do |comment| %>
                  <% if comment.item == item %>
                    <div class="media comment">
                      <div class="media-left">
                        <a href="#">
                          <img src="http://systemout.net/images/Gravatar-icon.png" alt="" class="media-object comment-icon">
                        </a>
                      </div>
                      <div class="media-body">
                        <p class="comment">
                          <%= comment.contents %>
                        </p>
                        <p class="small comment-user-info pull-right">
                          <em>Posted by <%= comment.user.name || comment.user_id %> on <%= comment.created_at.to_time.strftime('%b %-d, %Y') %></em>
                        </p>
                      </div>
                      <hr>
                    </div>
                  <% end %>
                <% end %>
              </div>
            </div>
          </div>
      </div>
    <% end %>
  <% end%>

</div>
</body>
</html>
